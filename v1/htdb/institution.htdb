#define	index.html
	${mkHeader(section=Institution)}
	<img src="https://s3.amazonaws.com/assets.jazzbutcher.com/projects/Institution/Institution_Logo.gif" />
	${beg_tabs}
	${begTab(About)}
	<div class="annotation">
	</div>
	${endTab}
    ${begTab(Live)}
    ${doGigSearch(f=extras, q=institution)}
    ${endTab}
    ${begTab(Released)}
    <div class="annotation">
    </div>
    ${endTab}
	${end_tabs}
	${jbc_footer}
